// AUTO-GENERATED by gen_config.py -- DO NOT EDIT
// Source: engine_config.schema.json

#include <jni.h>
#include <algorithm>
#include "engine_config.hpp"
#include "bridge/bridge_service.hpp"

// Forward: provided by engine/app at link time
namespace engine {
    EngineConfig& activeConfig();
    BridgeService& activeBridge();
    void notifyConfigChange(ConfigKey key);
    void onConfigSideEffect(ConfigKey key);
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetInactivityFpsLimit(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(10), static_cast<uint32_t>(1000));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.inactivityFpsLimit = captured;
        engine::notifyConfigChange(engine::ConfigKey::INACTIVITY_FPS_LIMIT);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetMaxFps(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(10), static_cast<uint32_t>(1000));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.maxFps = captured;
        cfg.reflexDirty = true;
        engine::notifyConfigChange(engine::ConfigKey::MAX_FPS);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetOutputScale2x(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.outputScale2x = captured;
        cfg.needRecreate = true;
        engine::notifyConfigChange(engine::ConfigKey::OUTPUT_SCALE2X);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetVsync(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.vsync = captured;
        cfg.needRecreate = true;
        engine::notifyConfigChange(engine::ConfigKey::VSYNC);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetDlssQuality(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(0), static_cast<uint32_t>(4));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.dlssQuality = captured;
        cfg.needRecreate = true;
        engine::notifyConfigChange(engine::ConfigKey::DLSS_QUALITY);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetUpscalerMode(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = static_cast<uint32_t>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.upscalerMode = captured;
        cfg.needRecreate = true;
        engine::notifyConfigChange(engine::ConfigKey::UPSCALER_MODE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetUpscalerQuality(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = static_cast<uint32_t>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.upscalerQuality = captured;
        cfg.needRecreate = true;
        engine::notifyConfigChange(engine::ConfigKey::UPSCALER_QUALITY);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetUpscalerResOverride(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(33), static_cast<uint32_t>(100));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.upscalerResOverride = captured;
        cfg.needRecreate = true;
        engine::notifyConfigChange(engine::ConfigKey::UPSCALER_RES_OVERRIDE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetAreaLightsEnabled(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.areaLightsEnabled = captured;
        engine::notifyConfigChange(engine::ConfigKey::AREA_LIGHTS_ENABLED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetEonDiffuse(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.eonDiffuse = captured;
        engine::notifyConfigChange(engine::ConfigKey::EON_DIFFUSE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetGreedyMeshingEnabled(
    JNIEnv*, jclass, jboolean, jboolean /*write*/) {
    bool captured = false;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.greedyMeshingEnabled = captured;
        engine::notifyConfigChange(engine::ConfigKey::GREEDY_MESHING_ENABLED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetMultiScatterGGX(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.multiScatterGGX = captured;
        engine::notifyConfigChange(engine::ConfigKey::MULTI_SCATTER_GGX);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetNoiseLOD(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.noiseLOD = captured;
        engine::notifyConfigChange(engine::ConfigKey::NOISE_LOD);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetOmmBakerLevel(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(1), static_cast<uint32_t>(8));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.ommBakerLevel = captured;
        engine::notifyConfigChange(engine::ConfigKey::OMM_BAKER_LEVEL);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetOmmEnabled(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.ommEnabled = captured;
        cfg.needRecreate = true;
        engine::onConfigSideEffect(engine::ConfigKey::OMM_ENABLED);
        engine::notifyConfigChange(engine::ConfigKey::OMM_ENABLED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetPomEnabled(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.pomEnabled = captured;
        engine::notifyConfigChange(engine::ConfigKey::POM_ENABLED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetPomFadeDistance(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = std::clamp(value, 8.0f, 256.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.pomFadeDistance = captured;
        engine::notifyConfigChange(engine::ConfigKey::POM_FADE_DISTANCE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetPomHeightScale(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = std::clamp(value, 0.0f, 1.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.pomHeightScale = captured;
        engine::notifyConfigChange(engine::ConfigKey::POM_HEIGHT_SCALE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetPomRefinement(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(0), static_cast<uint32_t>(16));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.pomRefinement = captured;
        engine::notifyConfigChange(engine::ConfigKey::POM_REFINEMENT);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetPomSteps(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(4), static_cast<uint32_t>(128));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.pomSteps = captured;
        engine::notifyConfigChange(engine::ConfigKey::POM_STEPS);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetRayBounces(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(1), static_cast<uint32_t>(12));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.rayBounces = captured;
        engine::notifyConfigChange(engine::ConfigKey::RAY_BOUNCES);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetRestirBounceEnabled(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.restirBounceEnabled = captured;
        engine::notifyConfigChange(engine::ConfigKey::RESTIR_BOUNCE_ENABLED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetRestirCandidates(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(1), static_cast<uint32_t>(32));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.restirCandidates = captured;
        engine::notifyConfigChange(engine::ConfigKey::RESTIR_CANDIDATES);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetRestirEnabled(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.restirEnabled = captured;
        engine::notifyConfigChange(engine::ConfigKey::RESTIR_ENABLED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetRestirSimplifiedBRDF(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.restirSimplifiedBRDF = captured;
        engine::notifyConfigChange(engine::ConfigKey::RESTIR_SIMPLIFIED_BRDF);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetRestirSpatialRadius(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(1), static_cast<uint32_t>(64));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.restirSpatialRadius = captured;
        engine::notifyConfigChange(engine::ConfigKey::RESTIR_SPATIAL_RADIUS);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetRestirSpatialTaps(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(0), static_cast<uint32_t>(8));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.restirSpatialTaps = captured;
        engine::notifyConfigChange(engine::ConfigKey::RESTIR_SPATIAL_TAPS);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetRestirTemporalMClamp(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(0), static_cast<uint32_t>(100));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.restirTemporalMClamp = captured;
        engine::notifyConfigChange(engine::ConfigKey::RESTIR_TEMPORAL_M_CLAMP);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetRestirWClamp(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(0), static_cast<uint32_t>(1000));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.restirWClamp = captured;
        engine::notifyConfigChange(engine::ConfigKey::RESTIR_W_CLAMP);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetSerEnabled(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.serEnabled = captured;
        engine::notifyConfigChange(engine::ConfigKey::SER_ENABLED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetSerHintsEnabled(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.serHintsEnabled = captured;
        engine::notifyConfigChange(engine::ConfigKey::SER_HINTS_ENABLED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetShadowSoftness(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = std::clamp(value, 0.0f, 2.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.shadowSoftness = captured;
        engine::notifyConfigChange(engine::ConfigKey::SHADOW_SOFTNESS);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetSharcEnabled(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.sharcEnabled = captured;
        engine::notifyConfigChange(engine::ConfigKey::SHARC_ENABLED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetSharcRoughnessThreshold(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = std::clamp(value, 0.0f, 1.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.sharcRoughnessThreshold = captured;
        engine::notifyConfigChange(engine::ConfigKey::SHARC_ROUGHNESS_THRESHOLD);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetSharcSceneScale(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = std::clamp(value, 0.5f, 32.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.sharcSceneScale = captured;
        engine::notifyConfigChange(engine::ConfigKey::SHARC_SCENE_SCALE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetSimplifiedIndirect(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.simplifiedIndirect = captured;
        engine::notifyConfigChange(engine::ConfigKey::SIMPLIFIED_INDIRECT);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetBrightAdaptSpeed(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    float captured = std::clamp(static_cast<float>(value) / 10.0f, 0.1f, 5.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.brightAdaptSpeed = captured;
        engine::notifyConfigChange(engine::ConfigKey::BRIGHT_ADAPT_SPEED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCenterWeightStrength(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    float captured = std::clamp(static_cast<float>(value) / 100.0f, 0.0f, 1.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.centerWeightStrength = captured;
        engine::notifyConfigChange(engine::ConfigKey::CENTER_WEIGHT_STRENGTH);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetDarkAdaptSpeed(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    float captured = std::clamp(static_cast<float>(value) / 10.0f, 0.1f, 5.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.darkAdaptSpeed = captured;
        engine::notifyConfigChange(engine::ConfigKey::DARK_ADAPT_SPEED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetExposureCompensation(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    float captured = std::clamp(static_cast<float>(value) / 10.0f, -5.0f, 5.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.exposureCompensation = captured;
        engine::notifyConfigChange(engine::ConfigKey::EXPOSURE_COMPENSATION);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetHighlightWeight(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    float captured = std::clamp(static_cast<float>(value) / 100.0f, 0.0f, 1.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.highlightWeight = captured;
        engine::notifyConfigChange(engine::ConfigKey::HIGHLIGHT_WEIGHT);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetManualExposure(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = std::clamp(value, 0.0001f, 10.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.manualExposure = captured;
        engine::notifyConfigChange(engine::ConfigKey::MANUAL_EXPOSURE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetManualExposureEnabled(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.manualExposureEnabled = captured;
        engine::notifyConfigChange(engine::ConfigKey::MANUAL_EXPOSURE_ENABLED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetSceneChangeThreshold(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    float captured = std::clamp(static_cast<float>(value) / 10.0f, 1.0f, 20.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.sceneChangeThreshold = captured;
        engine::notifyConfigChange(engine::ConfigKey::SCENE_CHANGE_THRESHOLD);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetLwhite(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    float captured = std::clamp(static_cast<float>(value) / 10.0f, 0.5f, 20.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.Lwhite = captured;
        engine::notifyConfigChange(engine::ConfigKey::LWHITE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCasSharpness(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    float captured = std::clamp(static_cast<float>(value) / 100.0f, 0.0f, 1.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.casSharpness = captured;
        engine::notifyConfigChange(engine::ConfigKey::CAS_SHARPNESS);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetColorExpansion(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    float captured = std::clamp(static_cast<float>(value) / 100.0f, 0.0f, 2.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.colorExpansion = captured;
        engine::notifyConfigChange(engine::ConfigKey::COLOR_EXPANSION);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetPsychoEnabled(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.psychoEnabled = captured;
        engine::notifyConfigChange(engine::ConfigKey::PSYCHO_ENABLED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetPsychoPeakSDR(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    float captured = std::clamp(static_cast<float>(value) / 100.0f, 0.5f, 3.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.psychoPeakSDR = captured;
        engine::notifyConfigChange(engine::ConfigKey::PSYCHO_PEAK_SDR);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetSaturation(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    float captured = std::clamp(static_cast<float>(value) / 100.0f, 0.0f, 2.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.saturation = captured;
        engine::notifyConfigChange(engine::ConfigKey::SATURATION);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetSaturationAdaptive(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.saturationAdaptive = captured;
        engine::notifyConfigChange(engine::ConfigKey::SATURATION_ADAPTIVE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetSharpenerMode(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = static_cast<uint32_t>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.sharpenerMode = captured;
        engine::notifyConfigChange(engine::ConfigKey::SHARPENER_MODE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetTonemappingMode(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = static_cast<uint32_t>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.tonemappingMode = captured;
        engine::notifyConfigChange(engine::ConfigKey::TONEMAPPING_MODE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetBloomEnabled(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.bloomEnabled = captured;
        engine::notifyConfigChange(engine::ConfigKey::BLOOM_ENABLED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetBloomIntensity(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = std::clamp(value, 0.0f, 1.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.bloomIntensity = captured;
        engine::notifyConfigChange(engine::ConfigKey::BLOOM_INTENSITY);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetBloomThreshold(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = std::clamp(value, 0.0f, 2.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.bloomThreshold = captured;
        engine::notifyConfigChange(engine::ConfigKey::BLOOM_THRESHOLD);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetFrameGenEnabled(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.frameGenEnabled = captured;
        engine::notifyConfigChange(engine::ConfigKey::FRAME_GEN_ENABLED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetFrameGenMultiplier(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(2), static_cast<uint32_t>(4));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.frameGenMultiplier = captured;
        engine::notifyConfigChange(engine::ConfigKey::FRAME_GEN_MULTIPLIER);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetMaxFpsLimit(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(0), static_cast<uint32_t>(1000));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.maxFpsLimit = captured;
        engine::notifyConfigChange(engine::ConfigKey::MAX_FPS_LIMIT);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetReflexBoost(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.reflexBoost = captured;
        engine::notifyConfigChange(engine::ConfigKey::REFLEX_BOOST);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetReflexEnabled(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.reflexEnabled = captured;
        engine::notifyConfigChange(engine::ConfigKey::REFLEX_ENABLED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCloudAltitude(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.cloudAltitude = captured;
        engine::notifyConfigChange(engine::ConfigKey::CLOUD_ALTITUDE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCloudAmbientStrength(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.cloudAmbientStrength = captured;
        engine::notifyConfigChange(engine::ConfigKey::CLOUD_AMBIENT_STRENGTH);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCloudAtmosphereFadeDist(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.cloudAtmosphereFadeDist = captured;
        engine::notifyConfigChange(engine::ConfigKey::CLOUD_ATMOSPHERE_FADE_DIST);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCloudCellFrequency(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.cloudCellFrequency = captured;
        engine::notifyConfigChange(engine::ConfigKey::CLOUD_CELL_FREQUENCY);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCloudCoverage(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.cloudCoverage = captured;
        engine::notifyConfigChange(engine::ConfigKey::CLOUD_COVERAGE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCloudDebugMode(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = static_cast<uint32_t>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.cloudDebugMode = captured;
        engine::notifyConfigChange(engine::ConfigKey::CLOUD_DEBUG_MODE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCloudDensity(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.cloudDensity = captured;
        engine::notifyConfigChange(engine::ConfigKey::CLOUD_DENSITY);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCloudDetailStrength(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.cloudDetailStrength = captured;
        engine::notifyConfigChange(engine::ConfigKey::CLOUD_DETAIL_STRENGTH);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCloudNoiseScale(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.cloudNoiseScale = captured;
        engine::notifyConfigChange(engine::ConfigKey::CLOUD_NOISE_SCALE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCloudQuality(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(0), static_cast<uint32_t>(6));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.cloudQuality = captured;
        engine::notifyConfigChange(engine::ConfigKey::CLOUD_QUALITY);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCloudScatterOctaves(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = static_cast<uint32_t>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.cloudScatterOctaves = captured;
        engine::notifyConfigChange(engine::ConfigKey::CLOUD_SCATTER_OCTAVES);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCloudSpeed(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.cloudSpeed = captured;
        engine::notifyConfigChange(engine::ConfigKey::CLOUD_SPEED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCloudTemporalBlend(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.cloudTemporalBlend = captured;
        engine::notifyConfigChange(engine::ConfigKey::CLOUD_TEMPORAL_BLEND);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCloudThickness(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.cloudThickness = captured;
        engine::notifyConfigChange(engine::ConfigKey::CLOUD_THICKNESS);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCloudType(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.cloudType = captured;
        engine::notifyConfigChange(engine::ConfigKey::CLOUD_TYPE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetCloudWindAngle(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.cloudWindAngle = captured;
        engine::notifyConfigChange(engine::ConfigKey::CLOUD_WIND_ANGLE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetOfflineAccumEnabled(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.offlineAccumEnabled = captured;
        engine::notifyConfigChange(engine::ConfigKey::OFFLINE_ACCUM_ENABLED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetOfflineAperture(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.offlineAperture = captured;
        engine::notifyConfigChange(engine::ConfigKey::OFFLINE_APERTURE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetOfflineBounces(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = static_cast<uint32_t>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.offlineBounces = captured;
        engine::notifyConfigChange(engine::ConfigKey::OFFLINE_BOUNCES);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetOfflineDenoised(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.offlineDenoised = captured;
        engine::notifyConfigChange(engine::ConfigKey::OFFLINE_DENOISED);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetOfflineDlssEpochLength(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = static_cast<uint32_t>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.offlineDlssEpochLength = captured;
        engine::notifyConfigChange(engine::ConfigKey::OFFLINE_DLSS_EPOCH_LENGTH);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetOfflineFocalDistance(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.offlineFocalDistance = captured;
        engine::notifyConfigChange(engine::ConfigKey::OFFLINE_FOCAL_DISTANCE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetDisplacementQuality(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(0), static_cast<uint32_t>(4));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.displacementQuality = captured;
        engine::notifyConfigChange(engine::ConfigKey::DISPLACEMENT_QUALITY);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetTessFarDist(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.tessFarDist = captured;
        engine::notifyConfigChange(engine::ConfigKey::TESS_FAR_DIST);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetTessMaxLevel(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = static_cast<uint32_t>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.tessMaxLevel = captured;
        engine::notifyConfigChange(engine::ConfigKey::TESS_MAX_LEVEL);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetTessMidDist(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.tessMidDist = captured;
        engine::notifyConfigChange(engine::ConfigKey::TESS_MID_DIST);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetTessNearDist(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.tessNearDist = captured;
        engine::notifyConfigChange(engine::ConfigKey::TESS_NEAR_DIST);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetChunkBuildingBatchSize(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(1), static_cast<uint32_t>(32));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.chunkBuildingBatchSize = captured;
        engine::notifyConfigChange(engine::ConfigKey::CHUNK_BUILDING_BATCH_SIZE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetChunkBuildingTotalBatches(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    uint32_t captured = std::clamp(static_cast<uint32_t>(value), static_cast<uint32_t>(1), static_cast<uint32_t>(32));
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.chunkBuildingTotalBatches = captured;
        engine::notifyConfigChange(engine::ConfigKey::CHUNK_BUILDING_TOTAL_BATCHES);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetChunkCullDistance(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = std::clamp(value, 64.0f, 2048.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.chunkCullDistance = captured;
        engine::notifyConfigChange(engine::ConfigKey::CHUNK_CULL_DISTANCE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetChunkLodDistance(
    JNIEnv*, jclass, jfloat value, jboolean /*write*/) {
    float captured = std::clamp(value, 32.0f, 512.0f);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.chunkLodDistance = captured;
        engine::notifyConfigChange(engine::ConfigKey::CHUNK_LOD_DISTANCE);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetDiagFlags(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    auto captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.diagFlags = captured;
        engine::notifyConfigChange(engine::ConfigKey::DIAG_FLAGS);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetDiagLevel(
    JNIEnv*, jclass, jint value, jboolean /*write*/) {
    auto captured = value;
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.diagLevel = captured;
        engine::notifyConfigChange(engine::ConfigKey::DIAG_LEVEL);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetGpuDiagnostics(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.gpuDiagnostics = captured;
        engine::notifyConfigChange(engine::ConfigKey::GPU_DIAGNOSTICS);
    }});
}

extern "C" JNIEXPORT void JNICALL Java_com_radiance_v2_bridge_ConfigBridge_nativeSetValidationLayers(
    JNIEnv*, jclass, jboolean value, jboolean /*write*/) {
    bool captured = static_cast<bool>(value);
    engine::activeBridge().post(engine::CmdConfigPatch{[captured]() {
        auto& cfg = engine::activeConfig();
        cfg.validationLayers = captured;
        engine::notifyConfigChange(engine::ConfigKey::VALIDATION_LAYERS);
    }});
}
